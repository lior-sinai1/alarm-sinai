{
  "name": "alarm-sinai-hub",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone bridge process: Tuya motion sensor (via Tuya Cloud) -> Fibaro Home Center. Runs independently of server.js and the Android app; the sensor is never exposed to the app UI.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc -p ."
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
