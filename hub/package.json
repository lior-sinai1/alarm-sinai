{
  "name": "alarm-sinai-hub",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone bridge process: local Tuya motion sensor -> Fibaro Home Center. Runs independently of server.js and the Android app; the sensor is never exposed to the app UI.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc -p ."
  },
  "dependencies": {
    "tuyapi": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
